// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
    docSidebar: [
        'index',
        'install',
        'file-format',
        'running',
        'the-library',
        'commands'
    ],
};

export default sidebars;
